# Core ML stack
torch>=2.0
torchvision>=0.15          # paired with torch; used only for transforms.Compose in train.py

# pose-format library (install from local clone or PyPI)
pose-format>=0.12

# pose-format hard dependencies (listed explicitly for reproducibility)
numpy>=1.23
scipy>=1.9
simple-video-utils>=0.0.6  # required by pose-format

# Data handling
pandas>=1.5

# Augmentations (used in augmentations/__init__.py)
opencv-python>=4.7

# Utilities
tqdm>=4.64
scikit-learn>=1.1          # used in utils.py for train/val splitting
matplotlib>=3.6
